<!doctype html>
<html>
<head>
  <title>scuba diving - Google Search</title>
  <link rel="stylesheet" href="/fluid-image-grid.css">
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script>window.jQuery || document.write('<script src="/vendor/jquery-1.9.1.js"><\/script>')</script>
</head>
<body>
  <div class='fluid-image-grid' data-row-height="180" data-ma="12" data-eca="0.1" data-mrw="80" data-bge="false" data-isuf="0">
    <div class="fluid-image-grid-inner"><%= erb :results %></div>
  </div>

  <script src="/fluid-image-grid.js"></script>
  <script>
    $(function () {
      FluidImageGrid.init({
        resizeThrottleRate: 100, // how often to recalculate layout on resize (ms)
        scrollThrottleRate: 10, // how often to check if new results need to be loaded (ms)
        nTbnsPending: 8 // number of pending images to allow at once before loading more results
      });
    });
  </script>
</body>
</html>
